function add(num, ber) {
  return num + ber;
}
function multiply(num, ber) {
  return num * ber;
}
function subtract(num, ber) {
  return num - ber;
}
function divide(num, ber) {
  return num / ber;
}
